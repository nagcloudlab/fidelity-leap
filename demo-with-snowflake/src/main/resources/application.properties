spring.application.name=demo-with-snowflake

# JDBC URL (without credentials)
spring.datasource.url=jdbc:snowflake://AOSMQAL-AA48942.snowflakecomputing.com

# Credentials (separate properties)
spring.datasource.username=NAGABHUSHANAMN
spring.datasource.password=imNaga12121212

# Driver
spring.datasource.driver-class-name=net.snowflake.client.jdbc.SnowflakeDriver

# Snowflake Context Properties
spring.datasource.hikari.data-source-properties.warehouse=COMPUTE_WH
spring.datasource.hikari.data-source-properties.db=DEMO_DB
spring.datasource.hikari.data-source-properties.schema=PUBLIC
spring.datasource.hikari.data-source-properties.client_session_keep_alive=true

# Connection Pool Settings (optional but recommended)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000